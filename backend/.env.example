# Copia este archivo a .env y configura DATABASE_URL con los datos de tu base de datos PostgreSQL
# Ejemplo de URL PostgreSQL (reemplaza USER, PASSWORD, HOST, PORT y DBNAME):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"

# Si usas Docker con un servicio postgres llamado 'db' en la misma red:
# DATABASE_URL="postgresql://postgres:postgres@db:5432/parking?schema=public"

# NOTA: No incluimos credenciales reales en el repositorio. Crea un archivo .env a partir de este ejemplo
# en la carpeta `backend` antes de ejecutar las migraciones.

