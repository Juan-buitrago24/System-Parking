# Copia este archivo a .env y configura DATABASE_URL con los datos de tu base de datos PostgreSQL
# Ejemplo de URL PostgreSQL (reemplaza USER, PASSWORD, HOST, PORT y DBNAME):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"

# Si usas Docker con un servicio postgres llamado 'db' en la misma red:
# DATABASE_URL="postgresql://postgres:postgres@db:5432/parking?schema=public"

# NOTA: No incluimos credenciales reales en el repositorio. Crea un archivo .env a partir de este ejemplo
# en la carpeta `backend` antes de ejecutar las migraciones.

# ===== INTEGRACION DE IA - RECONOCIMIENTO DE PLACAS =====
# Token de PlateRecognizer (servicio gratuito con 2,500 llamadas/mes)
# Para obtener tu token gratuito:
# 1. Registrate en https://app.platerecognizer.com/accounts/signup/
# 2. Ve a https://app.platerecognizer.com/api/
# 3. Copia tu API token
# 4. Pegalo aqui (sin comillas)
PLATE_RECOGNIZER_TOKEN=your_token_here

# ALTERNATIVA: Si prefieres no usar API externa, el sistema puede usar Tesseract.js (OCR local)
# simplemente deja PLATE_RECOGNIZER_TOKEN vacio o comentalo. El reconocimiento local es gratis
# pero tiene menor precision (85% vs 95%+ de PlateRecognizer).
